model:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  permissions:
    presets:
    - everyone
  package: thesis
  schema-server: 'local://'
  use:
  - invenio
files:
  use:
  - invenio_files
  properties:
    metadata:
      properties:
        file-title:
          type: fulltext
  package: thesis
  schema-server: 'local://'